#pragma once

class Globals
{
public:
	static const int screenWidth = 900; //900
	static const int screenHeight = 700; //700

	static short int keypressed;
	static short int effectpressed;
	enum Move { moveF, moveB, moveL, moveR, moveU, moveD, rotU, rotD, rotL, rotR };


};